{erl_opts, [debug_info]}.

{deps, [
  {erljob, ".*", {git, "git://github.com/cooldaemon/erljob.git", "master"}}
]}.

{cover_enabled, true}.

{clean_files, [
  "logs/*",
  "test/*.beam",
  "doc/*",
  "erl_crash.dump"
]}.
